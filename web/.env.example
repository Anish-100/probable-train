# Copy to .env.local and fill in. For local dev, get these from `supabase status`.
# In production (Vercel) set these to your cloud project's URL + publishable key.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
